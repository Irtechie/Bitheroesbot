using System;
using System.Collections.Generic;
using System.Text;

namespace BitheroesBot.Enums
{

        //no need for flags can keep commands segmented rather than joined. 
        public enum DiscordCommand
        {
  
            //K.I.S.S.  Add more later, just putting framework together. 
            None = 0,
            VerifyInGamePlayer = 1,
            RequestWBGroup = 2,
            GetMyExtendedId = 3,
    }
    }

